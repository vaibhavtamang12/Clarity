# Latency and Cost Benchmark Report (Phase 23)

> ⚠️ SCAFFOLD — auto-generated by `scripts/run_latency_benchmark.py`.
> Run `make bench-latency` to replace this file with measured results.
> No numbers are hand-written in this project (Rule 9).

## Methodology

- **Benchmark**: Each question is run through the full RAG pipeline
- **Iterations**: 3 measurement iterations per question (after warmup)
- **Stages measured**: transform, retrieval, context, generation, total
- **Percentiles**: p50 (median), p95, p99 computed across all samples
- **Cost estimation**: Based on token usage × model pricing (configurable)
- **Latency sources**: Real measured values, not estimates from documentation

## Results

_Pending run. The generated report will contain:_
- Total latency comparison across configurations
- Per-stage latency breakdown (p50/p95/p99 for each stage)
- Token usage statistics (prompt/completion/total per query)
- Cost analysis (estimated cost per query)
- Bottleneck identification

## Expected Analysis

- **LLM generation** is typically the largest latency contributor (often 60-80% of total)
- **Retrieval** (dense + sparse + reranking) is usually 10-20% of total
- **Token usage** varies by context size and answer length
- **Cost per query** depends on model pricing and token usage

## Next Steps

Phase 29 will use this benchmark data to:
- Identify optimization opportunities (embedding batching, caching, context size)
- Profile the complete system under production-like load
- Document before/after performance for each optimization