# Generation Evaluation Report (Phase 22)

> ⚠️ SCAFFOLD — auto-generated by `scripts/run_generation_evaluation.py`.
> Run `make eval-generation` to replace this file with measured results.
> No numbers are hand-written in this project (Rule 9).

## Methodology

- **Evaluation**: Full RAG pipeline (retrieval + generation) run on each question
- **Judge**: LLM-as-judge for semantic metrics (faithfulness, correctness, relevance)
- **Citation correctness**: Reuses Phase 12 citation validation logic
- **Configurations** (incremental, so we can isolate each component's contribution):
  - **Baseline RAG**: Dense retrieval only
  - **Hybrid RAG**: Dense + Sparse retrieval
  - **Hybrid + Reranker**: Dense + Sparse + cross-encoder reranking
  - **Hybrid + Reranker + Query Rewriting**: Full stack (Phase 10 transformation)
- **Metrics**:
  - **Faithfulness**: Are claims in the answer supported by retrieved context?
  - **Answer correctness**: Does the answer match the reference answer?
  - **Answer relevance**: Does the answer address the question?
  - **Citation correctness**: Do citations actually support the claims?
  - **Context relevance**: Is the retrieved context relevant to the question?

## Results

_Pending run. The generated report will contain the comparison table, component contribution analysis, per-category breakdown, and interpretation._

## Expected Analysis

- **Hybrid retrieval** should improve context relevance by combining semantic + lexical search
- **Reranking** should improve faithfulness by refining the top-K context
- **Query rewriting** should improve relevance by clarifying conversational follow-ups
- **Unanswerable questions** test whether the system fabricates when evidence is absent

## Component Contribution

The report will show whether each additional component (hybrid, reranker, query rewriting)
actually improves generation quality. If a component improves retrieval but degrades
faithfulness, the report will surface that trade-off (Rule 2).